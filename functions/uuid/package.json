{
  "name": "nodejs-docs-samples-functions-uuid",
  "version": "0.0.1",
  "private": true,
  "license": "Apache-2.0",
  "author": "Google Inc.",
  "repository": {
    "type": "git",
    "url": "https://github.com/GoogleCloudPlatform/nodejs-docs-samples.git"
  },
  "engines": {
    "node": ">=8.0.0"
  },
  "scripts": {
    "deploy": "tsc && gcloud beta functions deploy getUuid --env-vars-file .env.yml --trigger-http --runtime nodejs8",
    "lint": "repo-tools lint",
    "logs": "gcloud beta functions logs read getUuid",
    "pretest": "npm run lint",
    "test": "ava -T 20s --verbose test/*.test.js"
  },
  "dependencies": {
    "pg": "^7.4.3",
    "uuid": "3.2.1"
  },
  "devDependencies": {
    "@google-cloud/nodejs-repo-tools": "2.2.1",
    "ava": "0.25.0",
    "semistandard": "^12.0.1",
    "sinon": "4.4.2"
  },
  "cloud-repo-tools": {
    "requiresKeyFile": true,
    "requiresProjectId": true
  }
}
